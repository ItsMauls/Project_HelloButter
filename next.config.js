/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:
    [
        'i.pinimg.com',
        'milkbarstore.com',
        'res.cloudinary.com'
    ]
      }
};

module.exports = nextConfig
